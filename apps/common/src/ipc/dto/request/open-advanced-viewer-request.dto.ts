import type { OpenAdvancedViewerRequest } from '@/ipc/ipc.types';

export class OpenAdvancedViewerRequestDto implements OpenAdvancedViewerRequest {
  // Optionally initial bounds or flags; placeholder for future use
}
