현재 formatted-docs-ai-translator 앱에는 텍스트 번역만 있어. 이제는 이미지 번역을 새로 만들 거야. 기존 구조를 최대한 유지하는 것이 목표해야 해. 단, 이미지의 특성상 프롬프트 등의 부분에는 의도적인 변화가 있을 거야. 특히, 파싱(parseChannel)을 수행하지 않고, 번역(translateChannel) 과정에서 파싱과 번역이 동시에 수행될 거야. 물론 조립(applyChannel)는 정상적으로 수행할 거야.
따라서 @/docs 를 참고하며 작업하되, 이미지에 특화된 내용은 새로운 문서로 남겨줘.
또, ocr을 위해서, 다음 json response를 사용하게 해줘.

```json
{
  "type": "object",
  "properties": {
    "ocr_result": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "rich_text": {
            "type": "string",
            "description": "html styled text"
          },
          "box_2d": {
            "type": "array",
            "description": "bounding box. merge near and related boxes",
            "items": {
              "type": "number"
            }
          },
          "label": {
            "type": "string"
          },
          "language": {
            "type": "string"
          },
          "font_size": {
            "type": "number"
          }
        },
        "required": [
          "rich_text",
          "box_2d",
          "label",
          "language",
          "font_size"
        ]
      }
    },
    "translated_result": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "rich_text": {
            "type": "string",
            "description": "html styled text"
          },
          "box_2d": {
            "type": "array",
            "description": "bounding box. merge near and related boxes",
            "items": {
              "type": "number"
            }
          },
          "label": {
            "type": "string"
          },
          "language": {
            "type": "string"
          },
          "font_size": {
            "type": "number"
          }
        },
        "required": [
          "rich_text",
          "box_2d",
          "label",
          "language",
          "font_size"
        ]
      }
    }
  },
  "required": [
    "ocr_result",
    "translated_result"
  ]
}
```

- parse channel: 아무 것도 하지 않음
- translate channel: openai 라이브러리를 사용, 위의 response schema와 함께 프롬프트를 통해서 응답을 받음
  - 이미지 전용 프롬프트도 프롬프트 프리셋에서 관리
  - 다만 번역 타입에 따른 구분 필드가 추가
    - type이 text, image라는 enum을 사용
    - 번역할 때는 타입에 맞는 프롬프트만 선택 가능
  - ai에게 요청을 날릴 때에는 response_format를 사용해서 json을 입력, 프롬프트에는 json 스키마 내용이 들어가지 않음
  - ocr은 전적으로 llm api에 의존. 한 번의 요청으로 ocr과 번역을 동시에 처리함.
- apply channel: 앞(translate channel)의 응답을 활용, 이미지를 재조립하여 결과물을 내보냄
