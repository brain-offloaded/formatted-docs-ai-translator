import { jsonConfig } from './json.config';
import { csvConfig } from './csv.config';
import { textConfig } from './text.config';

export const translationConfigs = [jsonConfig, csvConfig, textConfig];
